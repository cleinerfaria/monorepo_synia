{
  "name": "monorepo-synia",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-select.cjs",
    "dev:aurea": "npm run dev -w aurea",
    "dev:white-label": "npm run dev -w wl",
    "build": "npm run build --workspaces",
    "test": "npm run test:run --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
