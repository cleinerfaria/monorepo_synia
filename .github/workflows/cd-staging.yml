name: Deploy to Staging

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
    types: [closed]

permissions:
  contents: read

concurrency:
  group: staging-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build
        run: npm run build

  deploy-staging:
    name: Deploy to Staging
    needs: quality-gate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: homolog
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate staging environment secrets
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          VIDASYSTEM_SUPABASE_PROJECT_REF: ${{ secrets.VIDASYSTEM_SUPABASE_PROJECT_REF }}
          VIDASYSTEM_SUPABASE_DB_PASSWORD: ${{ secrets.VIDASYSTEM_SUPABASE_DB_PASSWORD }}
          WL_SUPABASE_PROJECT_REF: ${{ secrets.WL_SUPABASE_PROJECT_REF }}
          WL_SUPABASE_DB_PASSWORD: ${{ secrets.WL_SUPABASE_DB_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          echo "🔍 Validating staging environment secrets..."
          if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
            echo "❌ Missing SUPABASE_ACCESS_TOKEN"
            exit 1
          fi
          if [ -z "${VIDASYSTEM_SUPABASE_PROJECT_REF:-}" ]; then
            echo "❌ Missing VIDASYSTEM_SUPABASE_PROJECT_REF for staging"
            exit 1
          fi
          if [ -z "${WL_SUPABASE_PROJECT_REF:-}" ]; then
            echo "❌ Missing WL_SUPABASE_PROJECT_REF for staging"
            exit 1
          fi
          echo "✅ All staging secrets validated"

      - name: Deploy VidaSystem to Staging
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          VIDASYSTEM_SUPABASE_PROJECT_REF: ${{ secrets.VIDASYSTEM_SUPABASE_PROJECT_REF }}
          VIDASYSTEM_SUPABASE_DB_PASSWORD: ${{ secrets.VIDASYSTEM_SUPABASE_DB_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          echo "🚀 Deploying VidaSystem to Staging..."
          echo "Project: ${VIDASYSTEM_SUPABASE_PROJECT_REF}"

          supabase link --project-ref "${VIDASYSTEM_SUPABASE_PROJECT_REF}" --password "${VIDASYSTEM_SUPABASE_DB_PASSWORD}" --workdir packages/db-vidasystem --debug
          supabase db push --workdir packages/db-vidasystem --include-all --debug

          echo "✅ VidaSystem staging deployment completed"

      - name: Deploy White Label to Staging
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          WL_SUPABASE_PROJECT_REF: ${{ secrets.WL_SUPABASE_PROJECT_REF }}
          WL_SUPABASE_DB_PASSWORD: ${{ secrets.WL_SUPABASE_DB_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          echo "🚀 Deploying White Label to Staging..."
          echo "Project: ${WL_SUPABASE_PROJECT_REF}"

          supabase link --project-ref "${WL_SUPABASE_PROJECT_REF}" --password "${WL_SUPABASE_DB_PASSWORD}" --workdir packages/db-whitelabel --debug
          supabase db push --workdir packages/db-whitelabel --include-all --debug

          echo "✅ White Label staging deployment completed"

      - name: Deploy White Label Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          WL_SUPABASE_PROJECT_REF: ${{ secrets.WL_SUPABASE_PROJECT_REF }}
        shell: bash
        run: |
          set -euo pipefail
          echo "⚡ Deploying White Label Edge Functions..."
          supabase functions deploy --workdir packages/db-whitelabel --project-ref "${WL_SUPABASE_PROJECT_REF}"
          supabase functions deploy init-superadmin --workdir packages/db-whitelabel --project-ref "${WL_SUPABASE_PROJECT_REF}" --no-verify-jwt
          supabase functions deploy manage-user --workdir packages/db-whitelabel --project-ref "${WL_SUPABASE_PROJECT_REF}" --no-verify-jwt
          echo "✅ White Label Edge Functions deployed"

      - name: Deploy VidaSystem Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          VIDASYSTEM_SUPABASE_PROJECT_REF: ${{ secrets.VIDASYSTEM_SUPABASE_PROJECT_REF }}
        shell: bash
        run: |
          set -euo pipefail
          echo "⚡ Deploying VidaSystem Edge Functions..."
          supabase functions deploy manage-user --workdir packages/db-vidasystem --project-ref "${VIDASYSTEM_SUPABASE_PROJECT_REF}" --no-verify-jwt
          echo "✅ VidaSystem Edge Functions deployed"

      - name: Verify Staging Deployment
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          VIDASYSTEM_SUPABASE_DB_PASSWORD: ${{ secrets.VIDASYSTEM_SUPABASE_DB_PASSWORD }}
          WL_SUPABASE_DB_PASSWORD: ${{ secrets.WL_SUPABASE_DB_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          echo "🔍 Verifying staging deployment..."

          echo "VidaSystem migrations status:"
          supabase migration list --workdir packages/db-vidasystem

          echo "White Label migrations status:"
          supabase migration list --workdir packages/db-whitelabel

          echo "🎉 Staging deployment verified successfully!"
