/**
 * This example page has been removed as it contained only mock data.
 * For MultiSelect component usage examples, refer to the sales filter components
 * which use real data from the database.
 *
 * Real examples can be found in:
 * - components/sales/SalesFiltersBar.tsx
 * - hooks/useFilterOptions.ts
 */

export default function MultiSelectExample() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-gray-900 dark:text-white">Página removida</h1>
        <p className="mt-2 text-gray-600 dark:text-gray-400">
          Esta página de exemplo foi removida por conter apenas dados mockados.
        </p>
        <p className="mt-1 text-sm text-gray-500 dark:text-gray-400">
          Use os dashboards de vendas para ver exemplos reais do MultiSelect.
        </p>
      </div>
    </div>
  );
}
