#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "Erro: commit direto na branch 'main' não é permitido."
  echo "Crie uma branch e abra um Pull Request para integrar mudanças."
  exit 1
fi

exit 0
