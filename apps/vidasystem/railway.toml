[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile"
buildArgs = {APP_NAME = "vidasystem"}

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3